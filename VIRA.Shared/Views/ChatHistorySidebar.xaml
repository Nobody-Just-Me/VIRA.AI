<UserControl
    x:Class="VIRA.Shared.Views.ChatHistorySidebar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VIRA.Shared.Views"
    xmlns:models="using:VIRA.Shared.Models">

    <Grid x:Name="RootGrid" Visibility="Collapsed">
        <!-- Overlay -->
        <Border x:Name="Overlay"
                Background="#80000000" 
                Tapped="OnOverlayTapped"/>
        
        <!-- Sidebar -->
        <Border x:Name="SidebarPanel"
                Width="280" 
                HorizontalAlignment="Left"
                Background="#1a1f2e"
                BorderBrush="#FFFFFF14"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <Border Grid.Row="0" 
                        Background="#8b5cf6" 
                        Padding="20,16">
                    <TextBlock Text="Chat History" 
                               FontSize="18" 
                               FontWeight="SemiBold"
                               Foreground="White"/>
                </Border>
                
                <!-- New Chat Button -->
                <Button Grid.Row="1"
                        x:Name="NewChatButton"
                        Content="+ New Chat" 
                        Margin="16,16,16,8"
                        Background="#FFFFFF14"
                        Foreground="White"
                        CornerRadius="12"
                        HorizontalAlignment="Stretch"
                        Height="44"
                        Click="OnNewChatClick"/>
                
                <!-- Chat List -->
                <ScrollViewer Grid.Row="2" Margin="8,0">
                    <ItemsRepeater x:Name="ChatHistoryList" 
                                   ItemsSource="{x:Bind ChatSessions, Mode=OneWay}">
                        <ItemsRepeater.ItemTemplate>
                            <DataTemplate x:DataType="models:ChatSession">
                                <Border Margin="8,4" 
                                        Background="#FFFFFF08"
                                        CornerRadius="8"
                                        Padding="12"
                                        Tapped="OnChatItemTapped"
                                        Tag="{x:Bind}">
                                    <StackPanel>
                                        <TextBlock Text="{x:Bind Title}" 
                                                   FontSize="14" 
                                                   Foreground="White"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{x:Bind FormattedTimestamp}" 
                                                   FontSize="11" 
                                                   Foreground="#94a3b8"
                                                   Margin="0,4,0,0"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsRepeater.ItemTemplate>
                    </ItemsRepeater>
                </ScrollViewer>
                
                <!-- Clear History -->
                <Button Grid.Row="3"
                        x:Name="ClearHistoryButton"
                        Content="Clear History" 
                        Margin="16"
                        Background="Transparent"
                        Foreground="#ef4444"
                        BorderBrush="#ef4444"
                        BorderThickness="1"
                        CornerRadius="12"
                        HorizontalAlignment="Stretch"
                        Height="44"
                        Click="OnClearHistoryClick"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
